# terraform-aws-nginx-server

## Security Note

Sensitive values such as allowed IP addresses are not committed to the repository.
Use `terraform.tfvars` or environment variables to provide them locally.
